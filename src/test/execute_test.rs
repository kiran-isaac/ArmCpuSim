use crate::{Executor, I, IT, ProcessorState};

#[test]
fn exit_syscall_test() {
    // let syscall = I {
    //     it: IT::SVC,
    //     immu:0,
    //     imms: 0,
    //     rn: 0,
    //     rd: 0,
    //     rm: 0,
    //     rt: 0,
    //     rl: 0,
    //     setflags: false,
    // };

    // let mut executor0 = Executor::new();
    // executor0.assign(syscall);
    // let mut state = ProcessorState::new();
    // executor0.execute(&mut state);
    // assert_eq!(state.halt, true)
}